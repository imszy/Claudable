# Copy to `.env` on the server (used by docker compose).
#
# Required for Claude Code SDK features:
ANTHROPIC_API_KEY=
#
# Optional but recommended: 32+ chars random string used to encrypt stored tokens.
# Example: openssl rand -hex 32
ENCRYPTION_KEY=

